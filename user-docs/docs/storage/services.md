# Сервисы FTP и WebDAV

RusNAS поддерживает дополнительные протоколы доступа к файлам: FTP для передачи файлов и WebDAV для работы через веб-браузер и совместимые клиенты.

---

## Где найти

Откройте страницу **Хранилище** и перейдите на вкладку **"Сервисы"**. На странице отображаются две карточки: FTP и WebDAV.

---

## FTP

### Статус и управление

В верхней части карточки FTP отображается:

- **Статус сервиса** -- зелёный (запущен) или серый (остановлен)
- **Кнопки управления** -- "Запустить" / "Остановить"
- **Активные подключения** -- количество текущих FTP-сессий

### Настройка FTP

Нажмите **"Настройки"** на карточке FTP. Доступные параметры:

| Параметр | Описание |
|----------|----------|
| **Анонимный доступ** | Разрешить подключение без логина. По умолчанию выключен |
| **Запись для анонимных** | Разрешить анонимным пользователям загружать файлы. Работает только при включённом анонимном доступе |
| **Chroot** | Ограничить пользователей их домашними директориями. Рекомендуется оставить включённым |
| **Диапазон пассивных портов** | Порты для пассивного режима FTP. По умолчанию: 30000-30100 |

После изменения настроек нажмите **"Сохранить"**. Сервис перезапустится автоматически.

### Подключение к FTP

Используйте любой FTP-клиент (FileZilla, WinSCP, встроенный в Проводник Windows):

- **Хост:** IP-адрес RusNAS
- **Порт:** 21
- **Логин/пароль:** учётные данные пользователя RusNAS

!!! warning "Внимание"
    FTP передаёт данные и пароли в открытом виде. Используйте FTP только во внутренней сети. Для доступа через интернет используйте WebDAV с HTTPS или SMB через VPN.

---

## WebDAV

WebDAV позволяет работать с файлами через веб-протокол HTTP/HTTPS. Поддерживается монтирование как сетевого диска в Windows, macOS и Linux.

### Статус и управление

В верхней части карточки WebDAV отображается:

- **Статус сервиса** -- зелёный (запущен) или серый (остановлен)
- **Кнопки управления** -- "Запустить" / "Остановить"

### Настройка WebDAV

Нажмите **"Настройки"** на карточке WebDAV для изменения параметров подключения.

### Управление пользователями WebDAV

WebDAV использует собственную систему аутентификации (Digest), отдельную от системных учётных записей.

#### Добавление пользователя

1. В карточке WebDAV нажмите **"Пользователи"**
2. Нажмите **"+ Добавить пользователя"**
3. Введите имя пользователя и пароль
4. Нажмите **"Создать"**

#### Удаление пользователя

1. Найдите пользователя в списке
2. Нажмите кнопку **"Удалить"** рядом с именем
3. Подтвердите удаление

!!! note "Примечание"
    Пользователи WebDAV не связаны с системными пользователями RusNAS. Это отдельные учётные записи для доступа к файлам через WebDAV.

### Подключение к WebDAV

#### Windows

1. Откройте **Проводник** → **"Подключить сетевой диск"**
2. В поле адреса введите: `https://<IP-адрес-RusNAS>/webdav/`
3. Введите логин и пароль пользователя WebDAV

#### macOS

1. В Finder выберите **"Подключение к серверу"** (Cmd+K)
2. Введите: `https://<IP-адрес-RusNAS>/webdav/`
3. Введите учётные данные

#### Linux

```
sudo mount -t davfs https://<IP-адрес-RusNAS>/webdav/ /mnt/webdav
```

!!! tip "Совет"
    WebDAV через HTTPS -- безопасная альтернатива FTP для доступа к файлам через интернет. Для HTTPS необходим TLS-сертификат, который можно настроить на странице [Сеть](../network/interfaces.md).

---

**См. также:** [Общие папки SMB/NFS](shares.md) | [Файловый менеджер](filebrowser.md)
